# Auth0 - Get from https://manage.auth0.com
# Create a Regular Web Application in Auth0 Dashboard
# AUTH0_DOMAIN is your tenant domain (e.g. tenant.auth0.com or tenant.us.auth0.com)
# Add http://localhost:3000/auth/callback to Allowed Callback URLs
# Add http://localhost:3000 to Allowed Logout URLs
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
AUTH0_SECRET=
APP_BASE_URL=http://localhost:3000

# Supabase - Get from Project Settings > Database
# DATABASE_URL: Use pooler (6543) for app runtime, or direct (5432) for both
# DIRECT_URL: Use direct (5432) for migrations - required for db push
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres?sslmode=require"
